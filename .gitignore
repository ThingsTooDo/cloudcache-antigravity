# Environment files (protect against accidental commits)
**/.env
**/.env.*
!**/.env.example

# Local .env file
.env
.env.local

# Wrangler local config files
**/wrangler.local.toml
**/wrangler.toml.local
**/.wrangler.generated.toml
**/.dev.vars
**/.wrangler/
.wrangler/
**/.wrangler/tmp

# Validation reports (local only, auto-generated)
docs/reports/validation/validation-report-*.md
.last-validation-timestamp

## Yarn artifacts (ensure PNPM-only)
.pnp.cjs
.yarn/
.yarnrc.yml
**/node_modules/
node_modules/
**/dist/
dist/
apps/*/dist/
.DS_Store
Thumbs.db
**/pnpm-lock.yaml
**/deploy.yml
.github/workflows/deploy.yml
.cursor/plans/
.pnpm-store/

# Cloudflare
.wrangler/
.mf/

# Secrets
.dev.vars
.env.*

# Docusaurus
.docusaurus/
.slush/

# IDE-specific directories (MULTI-IDE SAFETY)
# Cursor IDE - ignore cache, track rules and config
.cursor/
!.cursor/rules/
!.cursor/rules/**
# Per-app Cursor rules (module-specific architecture)
apps/*/.cursor/
!apps/*/.cursor/rules/
!apps/*/.cursor/rules/**
# .cursorignore - TRACKED (shared ignore patterns)
# .cursorrules - TRACKED (points to truth file)

# Antigravity IDE - ignore cache, workflows tracked via .agent/
.antigravity/
# .antigravityignore - TRACKED (shared ignore patterns)
# Note: .agent/ is intentionally NOT ignored (workflows are tracked)

# Session Lock
.ide-session.lock

# IDE cache and temporary files
*.log
*.tmp
*~

# Migration source (child repository - not tracked in parent)
migration/
