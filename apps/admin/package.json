{
  "name": "@cloudcache/admin",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "build:bundle": "tsup",
    "dev": "wrangler dev --port 8787 --inspector-port 9230 --no-bundle"
  },
  "dependencies": {
    "@cloudcache/platform-env": "workspace:*",
    "@cloudcache/platform-logging": "workspace:*",
    "@cloudcache/platform-http": "workspace:*"
  },
  "devDependencies": {
    "git-rev-sync": "^3.0.2",
    "tsup": "^8.2.2",
    "typescript": "^5.6.0"
  }
}
