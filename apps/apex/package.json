{
  "name": "@cloudcache/apex",
  "private": true,
  "scripts": {
    "build": "tsup",
    "build:bundle": "tsup",
    "dev": "wrangler dev --port 8788 --inspector-port 9231 --no-bundle"
  },
  "devDependencies": {
    "tsup": "^8.2.2",
    "git-rev-sync": "^3.0.2"
  }
}
