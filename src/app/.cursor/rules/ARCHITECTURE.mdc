# Architecture Rules: app module

Scope: `src/app`

- Keep public API in `index.ts` minimal and typed.
- No cross-imports from `src/admin` or `src/apex`.
- Side-effect-free modules; avoid top-level I/O.
- Prefer pure functions; isolate environment-specific code behind interfaces.
- Unit tests should mock external services.

